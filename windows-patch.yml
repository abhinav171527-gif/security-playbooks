---
- name: Sentinel360 Automated Patch Management
  hosts: all
  tasks:

    # Windows Patching
    - name: Install Windows Security Updates
      win_updates:
        category_names:
          - SecurityUpdates
          - CriticalUpdates
        state: installed
      when: ansible_os_family == "Windows"
